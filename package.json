{
  "name": "safe-promise",
  "version": "1.4.0",
  "description": "ES6 Promise wrapper that takes care of handling errors instead of swallowing them.",
  "repository": "https://github.com/toms-dev/safe-promise",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "typings install && tsc && typings bundle --out dist/typings --name safe-promise ;",
    "test": "mocha"
  },
  "files": [
    "dist"
  ],
  "author": "Tom Guillermin <tom@tomsdev.com>",
  "license": "ISC",
  "dependencies": {
    "mocha": "^2.5.3",
    "typescript": "^1.8.9",
    "typings": "^0.7.11"
  }
}
